position_controller:
  joints: {
    type: string_array,
    default_value: [],
    description: "Specifies joints used by the controller.",
    read_only: true,
  }
  command_interface_name: {
    type: string,
    default_value: "",
    description: "Name of the interface used for command.",
    read_only: true,
  }
  state_interface_name: {
    type: string,
    default_value: "",
    description: "Name of the interface used for state."
  }